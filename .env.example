# WalletConnect Project ID
# 1. Sign up at https://cloud.walletconnect.com (free)
# 2. Click "Create" to make a new project (any name works)
# 3. Copy the "Project ID" from the project dashboard
#
# Not strictly required — direct wallet connections (e.g. Rabby) work without it,
# but WalletConnect QR code pairing won't.
PUBLIC_WALLETCONNECT_PROJECT_ID=

# ChatRoom Contract Address
# Deploy the ChatRoom contract to Base Sepolia using Foundry (see README.md for steps)
# Paste the deployed address here
#
# If you're unable to deploy your own, you can use this pre-deployed contract:
# 0x59a65f35eeAC6211adD3f5399bEd71D782bcf5e6
PUBLIC_CHAT_ROOM_CONTRACT_ADDRESS=

# Alchemy API Key
# The default public RPC (sepolia.base.org) is unreliable, so we use Alchemy instead.
# 1. Sign up at https://dashboard.alchemy.com (free)
# 2. Create a new app, select "Base Sepolia" as the network
# 3. Copy the API key from the app dashboard
PUBLIC_ALCHEMY_API_KEY=
